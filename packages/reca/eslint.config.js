import rootStyles from "./../../eslint.config.js";

/** @type {import("eslint").Linter.Config} */
export default [
    ...rootStyles,
    {
        rules: {
            // add rules here
        }
    }
];
